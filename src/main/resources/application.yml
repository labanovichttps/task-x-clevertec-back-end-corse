spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: loverloser
    url: jdbc:postgresql://localhost:5432/certificate_repository
  jpa:
    show-sql: true